/**
 * Filter falsy values (false, null, undefined) from an array
 * @param  {Array<Any>} arr an array containing any type of index
 * @return {Array<Any>} the filtered array containing only truthy values
 */
export default arr => arr.filter(id => id);
