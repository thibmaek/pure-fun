/**
 * Join a series of arrays into a new array
 * @param  {...Array<Any>} arrs a series of arrays to join
 * @return {Array<Array[Any>} a newly (nested) join of arrays
 */
export default (...arrs) => [...arrs];